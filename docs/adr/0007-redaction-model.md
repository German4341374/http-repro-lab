# ADR 0007: Use stable typed placeholders

Status: Accepted

Headers and values are replaced with typed placeholders such as `${AUTH_TOKEN}` and `EMAIL_001`. Stable pseudonyms preserve correlations within one analysis without retaining the original value.

