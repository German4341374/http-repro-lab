# ADR 0003: Local-first operation

Status: Accepted

HTTP evidence commonly contains credentials and personal data. Core workflows therefore require no cloud service, telemetry is disabled, and local servers bind to loopback by default.

